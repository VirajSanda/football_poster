def generate_summary(text: str) -> str:
    """Fake summary generator (replace with ML/NLP later)."""
    return text[:150] + "..." if len(text) > 150 else text


def generate_full_description(text: str) -> str:
    """Stub for a longer description."""
    return text
